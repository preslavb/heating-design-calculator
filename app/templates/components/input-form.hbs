<!-- Create the components for the different input tabs and set their classes -->
{{site-inputs siteInput=siteInput}}
{{#each siteInput.rooms as |roomInput|}}
  {{room-inputs roomInput=roomInput createRoom=(action createRoom)
                addElement=(action roomInput.addElement) destroyElement=(action roomInput.destroyElement)
                addFloor=(action roomInput.addFloor) destroyFloor=(action roomInput.destroyFloor)
                addPortal=(action roomInput.addPortal) destroyPortal=(action roomInput.destroyPortal)}}
{{/each}}
{{emitter-specifications emitterSpecifications=emitterSpecifications}}
